# RevengeOS devices bot (based on HomeBot)

- Execute `pip3 install .` to install all the dependencies

- Put a bot token in `config.env`

- Launch the bot by typing 

  ```bash
  python3 -m homebot
  ```